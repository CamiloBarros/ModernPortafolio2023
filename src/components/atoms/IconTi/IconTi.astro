---
interface Props {
  name : string
  customClass ?: string
  dataOpenMenu ?: string
}
const { name, customClass = '', dataOpenMenu } = Astro.props
---

<i class=`ti ti-${name} ${customClass}` data-custom-attribute={dataOpenMenu} ></i>