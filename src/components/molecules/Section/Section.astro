---
interface Props {
  customClass?: string,
  bgColor?: string // white, [#333333], etc...
  id ?: string

}

const { customClass = '', bgColor = 'white', id = '' } = Astro.props
---

<section class=`bg-${bgColor} py-20 px-10 relative` id={id}>
  <div class=`${customClass}`>
    <slot />
  </div>
</section>